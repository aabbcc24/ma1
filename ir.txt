#include<msp430.h>

int main(){
  WDTCTL=WDTPW |WDTHOLD;
  P1DIR |= BIT1; //BUZZER
  P1DIR &= ~BIT4; // IR OUTPUT
  P1OUT &= ~BIT1;

  while(1){
    if(!(P1IN & BIT4)){
      P1OUT |=BIT1;
      
      }
      else{
        P1OUT &= ~BIT1;
        
      }
    
    }
    return  0;
    
  
  }
